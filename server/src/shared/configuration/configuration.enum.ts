export enum Configuration {
  HOST = 'HOST',
  PORT = 'PORT',
  MONGO_URI = 'MONGO_URI',
  JWT_KEY = 'JWT_KEY',
  UPLOAD_PATH = 'UPLOAD_PATH',
  MAX_FILE_SIZE = 'MAX_FILE_SIZE',
  NODE_ENV = 'NODE_ENV',
}
